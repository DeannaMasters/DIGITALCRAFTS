userCommand = ""
shoppingList = []
groceryItem = {}

print("\nWelcome to your Grocery App!\nPlease select an option:")

choice = input("\nPress 1 to ADD STORE\nPress 2 to ADD GROCERY ITEM\nPress 3 to VIEW TASK LIST\nPress 4 to DELETE ITEM\nPress q to QUIT LIST\nPlease select your choice:")

# stop = False
# while not stop:

    
    # while userCommand != "q":
    #     userCommand = input(mainMenu)

# stop = False
# while not stop:

    # if userCommand == "1":
    #     storeName = input("Store name:\n")
    
    # if userCommand == "2":
    #     productName = input("Product name:\n")
    
    # quantity = input("Quantity:\n")
    
    # cost = input("Cost:\n")
    
    # grocery_item = {'StoreName':storeName, 'ProductName':productName, 'Quantity':int(quantity), 'cost':float(cost)}
    
    # shoppingList.append(groceryItem)
    
while userCommand != "q":
        #  stop = True

    if userCommand == "1":
        storeName = input("Store name:\n")
        storeName = input("Which store would you like to add?:  ")
        # storeIndex = len(shoppingList) + 1
        # shoppingListToDo = str(shoppingList) + ": " + storeName + "  -  "
        # shoppingList.append(shoppingList)

        # print("\n" + storeName + " is added to your Grocery App!")

    if userCommand == "2":
        # groceryItem = input("Which item would you like to add?:  ")
        # groceryIndex = len(shoppingList) + 1
        # shoppingListToDo = str(shoppingList) + ": " + groceryItem + "  -  "
        # shoppingList.append(shoppingList)

        # quantity = input("Quantity:\n")
    
    #     cost = input("Cost:\n")
    
    # grocery_item = {'StoreName':storeName, 'ProductName':productName, 'Quantity':int(quantity), 'cost':float(cost)}
    
    #     shoppingList.append(groceryItem)

    # print("\n" + storeName + " is added to your Grocery App!")    

    # if userCommand == "3":
    #     print("Here is your SHOPPING LIST:")
    #     print(shoppingList)

    # if userCommand == "4":
    #     print("DELETION\nHere is your grocery list:\n", shoppingList)
    #     Delete = int(input("Which item would you like remove?\n (PLEASE INPUT CORRISPONDING NUMERICAL CHOICE!)" )) - 1
    #     print(f"\nTODO {shoppingList[groceryItemdelete]}   :Has been DELETED!\n")
    #     del shoppingList[groceryItemDelete]
    #     print("\n Returning to MAIN MENU)   
    
  